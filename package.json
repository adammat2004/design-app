{
  "name": "garden-studio",
  "private": true,
  "packageManager": "pnpm@11.24.0",
  "engines": {
    "node": ">=20 <25"
  },
  "scripts": {
    "setup": "./script/setup",
    "dev": "pnpm --filter @garden-studio/schema build && concurrently -n schema,api,web -c magenta,blue,green \"pnpm --filter @garden-studio/schema dev\" \"pnpm --filter @garden-studio/api dev\" \"pnpm --filter @garden-studio/web dev\"",
    "build": "pnpm --filter @garden-studio/schema build && pnpm --filter @garden-studio/api build && pnpm --filter @garden-studio/web build",
    "lint": "pnpm -r lint",
    "test": "pnpm -r test",
    "test:e2e": "pnpm --filter @garden-studio/web test:e2e",
    "render:material": "pnpm --filter @garden-studio/web render:material",
    "db:migrate": "pnpm --filter @garden-studio/api db:migrate",
    "format": "prettier --write .",
    "format:check": "prettier --check ."
  },
  "devDependencies": {
    "concurrently": "^9.1.0",
    "prettier": "^3.4.2"
  }
}
